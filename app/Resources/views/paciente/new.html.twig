{% extends 'base.html.twig' %}

{% block body %}
    <h1>Crear nuevo Paciente</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('paciente_index') }}">Volver a la lista</a>
        </li>
    </ul>
{% endblock %}
